import { Theme } from 'common/interfaces';

const DEFAULT_THEME: Theme = {
  primaryColor: '#CDD1C4',
  primaryBackgroundColor: '#30323D',
  additionalBackgroundColor: '#5C80BC',
};

export { DEFAULT_THEME };
