import { Settings } from 'common/interfaces';

const DEFAULT_SETTINGS: Settings = {
  logLevel: 'info',
  terminal: {
    fontFamily: 'Consolas',
    fontSize: 15,
  },
  theme: 'Dark',
};

export { DEFAULT_SETTINGS };
